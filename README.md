# Personal Portfolio

This is a minimal, responsive portfolio template meant for GitHub Pages.

## Local preview

```bash
npx serve .
```

## Deploy

1. Commit & push to the `main` branch.
2. Enable GitHub Pages in repo **Settings → Pages** (`main` / root).
3. Optionally add a custom domain and tick **Enforce HTTPS**.
